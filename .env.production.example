# =============================================================================
# PRODUCTION ENVIRONMENT VARIABLES
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# Подключение к существующей PostgreSQL на хост-машине
# Docker может обращаться к хосту через host.docker.internal (Linux requires extra setup)
# или по IP сервера
DATABASE_URL=postgresql+asyncpg://postgres_user:postgres_password@host.docker.internal:5432/instagram_tracker

# -----------------------------------------------------------------------------
# Worker Configuration
# -----------------------------------------------------------------------------
WORKER_INTERVAL_HOURS=6
WORKER_REELS_LIMIT=50
AUDIO_DIR=audio

# -----------------------------------------------------------------------------
# Instagram Authentication
# -----------------------------------------------------------------------------
INSTAGRAM_SESSIONID=your_production_sessionid
INSTAGRAM_CSRFTOKEN=your_production_csrftoken

# -----------------------------------------------------------------------------
# OPTIONAL
# -----------------------------------------------------------------------------
# INSTAGRAM_PROXY=http://user:pass@proxy_host:proxy_port
# TEST_MODE=false
