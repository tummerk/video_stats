# Тестовый Режим (TEST_MODE)

## Описание

Тестовый режим позволяет быстро проверить работу `unified_worker.py` без ожидания нескольких часов. Все задачи запускаются с короткими интервалами.

---

## Как Включить

Добавьте в `.env` файл:
```bash
TEST_MODE=true
```

---

## Интервалы в Тестовом Режиме

| Задача | Обычный режим | Тестовый режим |
|--------|---------------|----------------|
| Fetch videos | Каждые 6 часов | **Каждые 10 секунд** |
| Update schedules | Каждые 1 час | **Каждые 30 секунд** |
| Process metrics | Каждые 1 минуту | **Каждые 10 секунд** |

---

## Пример Запуска

### 1. Отредактируйте .env
```bash
# Добавьте или измените:
TEST_MODE=true
```

### 2. Запустите worker
```bash
python unified_worker.py
```

### 3. Ожидаемый вывод
```
============================================================
STARTING UNIFIED WORKER
============================================================
⚠️  TEST MODE ENABLED - Short intervals for testing!
⚠️  Set TEST_MODE=false in .env for production use
  - Process metrics: every 10 seconds (TEST MODE)
  - Update schedules: every 30 seconds (TEST MODE)
  - Fetch videos: every 10 seconds (TEST MODE)
Scheduler started
Running initial tasks...
Starting fetch_new_videos task (Run #1)
⚠️  TEST MODE - Using reduced delays!
Found 1 accounts in database
Processing account: username (id=12345)
Job completed: processed 1 accounts

Checking for videos needing schedule updates (Run #1)...
Processing scheduled metrics collection (Run #1)...

WORKER IS RUNNING
Press Ctrl+C to stop

# Через 10 секунд:
Starting fetch_new_videos task (Run #2)
⚠️  TEST MODE - Using reduced delays!
...

# Через 10 секунд:
Starting fetch_new_videos task (Run #3)
...
```

---

## Счётчики Запусков

В тестовом режиме вы будете видеть номера запусков:

- `Starting fetch_new_videos task (Run #X)` - запуск задачи получения видео
- `Checking for videos needing schedule updates (Run #X)` - запуск обновления расписаний
- `Processing scheduled metrics collection (Run #X)` - запуск сбора метрик

---

## Предупреждения

⚠️ **ВАЖНО**: Тестовый режим НЕ предназначен для продакшена!

- Очень частые запросы к Instagram API
- Может вызвать rate limiting или блокировку
- Нагрузка на базу данных
- Используйте **только для тестирования**

---

## Как Вернуться в Обычный Режим

### Вариант 1: Удалить TEST_MODE из .env
Просто удалите строку `TEST_MODE=true` из `.env` файла

### Вариант 2: Установить в false
```bash
TEST_MODE=false
```

### Вариант 3: Закомментировать
```bash
# TEST_MODE=true
```

---

## Проверка Работы Цикла

### 1. Запустите в тестовом режиме
```bash
# Убедитесь, что в .env есть TEST_MODE=true
python unified_worker.py
```

### 2. Наблюдайте за выводом
Вы должны видеть каждые 10 секунд:
```
Starting fetch_new_videos task (Run #1)
...
Starting fetch_new_videos task (Run #2)
...
Starting fetch_new_videos task (Run #3)
```

### 3. Остановите по Ctrl+C
Когда убедились, что всё работает:
```
Press Ctrl+C to stop
^C
Shutting down...
Worker stopped
```

### 4. Вернитесь в обычный режим
```bash
# Измените в .env:
TEST_MODE=false

# Или удалите строку полностью
```

---

## Пример Сценария Тестирования

### Сценарий 1: Быстрая проверка цикла
```bash
# 1. Включить тестовый режим в .env
echo "TEST_MODE=true" >> .env

# 2. Запустить worker на 30 секунд
timeout 30 python unified_worker.py

# 3. Увидеть 3-4 запуска fetch_new_videos
# 4. Выключить тестовый режим
sed -i 's/TEST_MODE=true/TEST_MODE=false/' .env
```

### Сценарий 2: Проверка с логированием
```bash
# Запустить с сохранением логов
python unified_worker.py 2>&1 | tee test_run.log

# Подождать 1 минуту (увидеть ~6 запусков)
# Нажать Ctrl+C

# Посмотреть логи
grep "Run #" test_run.log
```

### Сценарий 3: Мониторинг базы данных
```bash
# В одной консоли - worker
TEST_MODE=true python unified_worker.py

# В другой консоли - мониторинг БД
watch -n 5 'psql -U user -d dbname -c "SELECT COUNT(*) FROM videos;"'
```

---

## Сравнение Режимов

### Обычный режим (TEST_MODE=false или не задан)
```
Starting fetch_new_videos task (Run #1)
# ... подождать 6 часов ...
Starting fetch_new_videos task (Run #2)
```

### Тестовый режим (TEST_MODE=true)
```
Starting fetch_new_videos task (Run #1)      # 0:00
Starting fetch_new_videos task (Run #2)      # 0:10
Starting fetch_new_videos task (Run #3)      # 0:20
Starting fetch_new_videos task (Run #4)      # 0:30
```

---

## Возможные Проблемы

### Проблема: Слишком много логов
**Решение**: Уменьшите level логирования в `unified_worker.py`:
```python
logging.basicConfig(
    level=logging.WARNING,  # вместо INFO
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s'
)
```

### Проблема: Rate limiting от Instagram
**Решение**:
- Увеличьте интервалы в коде
- Или уменьшите `WORKER_REELS_LIMIT` в `.env`
- Или используйте mock тесты вместо реальных API

### Проблема: Worker не перезапускается после изменения .env
**Решение**: Worker нужно перезапустить после изменения `.env`:
```bash
# Остановить: Ctrl+C
# Перезапустить:
python unified_worker.py
```

---

## Полезные Команды

### Проверить текущий режим
```bash
grep TEST_MODE .env
```

### Быстро включить тестовый режим
```bash
echo "TEST_MODE=true" >> .env
```

### Быстро выключить тестовый режим
```bash
sed -i '/TEST_MODE=true/d' .env
# или
sed -i 's/TEST_MODE=true/TEST_MODE=false/' .env
```

### Запустить на 1 минуту в тестовом режиме
```bash
# Linux/Mac:
TEST_MODE=true timeout 60 python unified_worker.py

# Windows (PowerShell):
$env:TEST_MODE="true"; python unified_worker.py
# (нажать Ctrl+C через 1 минуту)
```

---

## Дополнительные Интервалы

Если вам нужны другие интервалы для тестирования, отредактируйте `unified_worker.py`:

```python
# В функции main():
if test_mode:
    metrics_interval = 10      # Изменить на нужное количество секунд
    schedules_interval = 30    # Изменить на нужное количество секунд
    videos_interval = 10       # Изменить на нужное количество секунд
```

---

## Резюме

✅ **Используйте TEST_MODE=true для:**
- Быстрой проверки worker
- Отладки логики
- Тестирования изменений

❌ **НЕ используйте TEST_MODE=true для:**
- Продакшена
- Длительной работы
- Аккаунтов с большим количеством видео

🔄 **Всегда возвращайтесь в обычный режим** после тестирования!
